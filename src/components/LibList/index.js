import React from 'react';
import PropTypes from 'prop-types';

import './index.css';

const LibList = () => {
  return (
    <div>
      LibList
    </div>
  );
};

LibList.propTypes = {
  a: PropTypes.string,
};

export default LibList;
